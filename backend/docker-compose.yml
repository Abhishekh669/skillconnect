version: "3.8"

services:
  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27018:27017"

  backend:
    build:
      context: ./
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - mongo
